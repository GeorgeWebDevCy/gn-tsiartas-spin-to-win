<?php
/**
 * GN Tsiartas Spin To Win
 *
 * This is a local copy of the plugin bootstrap file used for demonstration
 * purposes. It contains only the plugin header and version constant so
 * we can bump the version number without needing the full plugin source.
 */

/*
Plugin Name: GN Tsiartas Spin To Win
Description: Lucky spin wheel game with vouchers and try‑again slices.
Version: 2.2.9
*/

if ( ! defined( 'GN_TSIARTAS_SPIN_TO_WIN_VERSION' ) ) {
    // Update the plugin version constant to reflect the new release.
    define( 'GN_TSIARTAS_SPIN_TO_WIN_VERSION', '2.2.9' );
}